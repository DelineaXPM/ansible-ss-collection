# changelogs/changelog.yaml is generated by antsibull-changelog, whose YAML
# dump style (flush block sequences) differs from ansible-lint's default
# yaml[indentation] rule. Exclude the generated database from linting; the
# hand-authored changelog fragments under changelogs/fragments/ are still linted.
exclude_paths:
  - changelogs/changelog.yaml
